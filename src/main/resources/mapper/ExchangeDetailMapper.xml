<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sinognss.cloud.vantix.infrastructure.mapper.ExchangeDetailMapper">
    <insert id="insertBatch">
        INSERT INTO exchange_detail (
            exchange_batch_id, detail_index, service_code_id, request_id,
            service_code_snapshot, status, created_at, updated_at
        ) VALUES
        <foreach collection="details" item="item" separator=",">
            (
                #{item.exchangeBatchId}, #{item.detailIndex}, #{item.serviceCodeId}, #{item.requestId},
                #{item.serviceCodeSnapshot}, #{item.status}, #{item.createdAt}, #{item.updatedAt}
            )
        </foreach>
    </insert>
</mapper>
