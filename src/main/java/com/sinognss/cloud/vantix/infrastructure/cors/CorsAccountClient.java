package com.sinognss.cloud.vantix.infrastructure.cors;

/**
 * Second-stage CORS boundary. No HTTP implementation is provided until the CORS contract exists.
 */
public interface CorsAccountClient {
    // TODO(second-stage): create, renew and force-activate accounts through the CORS service.
}
